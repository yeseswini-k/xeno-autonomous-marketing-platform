{
  "name": "xeno-crm-monorepo",
  "version": "1.0.0",
  "description": "AI-Native Mini CRM & Channel Service Workspace",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm run install:backend && npm run install:frontend",
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix frontend",
    "dev": "concurrently --kill-others \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix backend",
    "test:simulation": "npm run test:simulation --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
